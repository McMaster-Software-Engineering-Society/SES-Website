---
import { getCollection } from "astro:content";
import JobListItem from "../jobs-list-item.astro";

const newGradLists = await getCollection("newGradLists");
---

<div class="flex flex-col gap-y-4">
  {newGradLists.map((posting) => <JobListItem job={posting} />)}
</div>
