---
import MerchItemQuantityIndicator from "./merch-item-quantity-indicator.astro";

export interface Props {
  imageSrc: string;
  name: string;
  price: number;
  quantities: {
    small?: number;
    medium?: number;
    large?: number;
    xlarge?: number;
  };
}

const { imageSrc, name, price, quantities } = Astro.props;

const sizeMap = {
  small: "SM",
  medium: "MD",
  large: "LG",
  xlarge: "XL",
};

const sizeMapFull = {
  small: "SMALL",
  medium: "MEDIUM",
  large: "LARGE",
  xlarge: "X-LARGE",
};
---

<div class="flex flex-col">
  <img src={imageSrc} class="rounded-2xl" />
  <div class="flex flex-col mt-4">
    <p class="font-bold">{name}</p>
    <p class="lg:hidden block">${price}</p>
  </div>
  <div
    class="flex flex-col lg:flex-row items-start lg:items-center justify-between gap-y-2 mt-1 lg:mt-0">
    <!-- Price -->
    <p class="hidden lg:block">${price}</p>
    <!-- Size Availability -->
    <div
      class="grid grid-cols-2 w-full lg:w-auto lg:flex lg:flex-row lg:items-center gap-x-3">
      <MerchItemQuantityIndicator
        sizeLabel={sizeMap.small}
        sizeLabelFull={sizeMapFull.small}
        quantity={quantities.small ?? 0}
      />
      <MerchItemQuantityIndicator
        sizeLabel={sizeMap.medium}
        sizeLabelFull={sizeMapFull.medium}
        quantity={quantities.medium ?? 0}
      />
      <MerchItemQuantityIndicator
        sizeLabel={sizeMap.large}
        sizeLabelFull={sizeMapFull.large}
        quantity={quantities.large ?? 0}
      />
      <MerchItemQuantityIndicator
        sizeLabel={sizeMap.xlarge}
        sizeLabelFull={sizeMapFull.xlarge}
        quantity={quantities.xlarge ?? 0}
      />
    </div>
  </div>
</div>
