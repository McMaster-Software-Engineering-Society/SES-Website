---
import { socialMedia } from "data/social-media";
---

<div class="flex flex-col gap-y-4">
  <h1 class="text-center text-lg font-medium">Connect with us!</h1>
  <div id="social-media" class="flex flex-row gap-x-4 justify-center">
    {
      socialMedia.map((media) => (
        <a href={media.url} class="text-3xl hover:scale-110 transition-all rounded-full p-3 hover:bg-white/10" target="_blank">
          <media.icon />
        </a>
      ))
    }
  </div>
</div>
