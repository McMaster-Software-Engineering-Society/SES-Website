---
import { socialMedia } from "data/social-media";
---

<div id="social-media" class="flex flex-row gap-x-4">
  {
    socialMedia.map((media) => (
      <a href={media.url} class="text-3xl">
        <media.icon />
      </a>
    ))
  }
</div>
